```{r Import libraries}
library(tidyverse)
library(rmarkdown)
library(rprojroot)
library(arrow)


source(file.path(find_root("paths.R"),"paths.R"))

```

```{r Check data types}
student_data <- read_parquet(file.path(raw_data,"student_data.parquet"))
unique_df <- bind_rows(lapply(data, function(x) distinct(data, !!sym(names(data)[which(data == x)[1]]))), .id = "variable")


```

